import React from 'react';
import ReactDOM from 'react-dom';
import expect from 'expect';
import $ from 'jQuery';
import TestUtils from 'react-dom/test-utils';
import NewsSingle from '../../components/NewsSingle.jsx';

describe('NewsSingle', () => {
  it('should exist', () => {
    expect(NewsSingle).toExist();
  });
});
