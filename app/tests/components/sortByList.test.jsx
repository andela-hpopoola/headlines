import React from 'react';
import ReactDOM from 'react-dom';
import expect from 'expect';
import $ from 'jQuery';
import TestUtils from 'react-dom/test-utils';
import SortByList from '../../components/SortByList.jsx';

describe('SortByList', () => {
  it('should exist', () => {
    expect(SortByList).toExist();
  });
});
