import React from 'react';
import ReactDOM from 'react-dom';
import expect from 'expect';
import $ from 'jQuery';
import SourcesOption from '../../components/SourcesOption.jsx';

describe('SourcesOption', () => {
  it('should exist', () => {
    expect(SourcesOption).toExist();
  });
});
